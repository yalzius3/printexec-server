node_modules/
dist/
.env
.env.local
.env.*.local
.DS_Store
*.log
uploads/*
!uploads/.gitkeep

# Throwaway Postgres data directories for npm run test:integration. One per run
# (.pgtest-<pid>), so a killed run can never block the next one by holding the
# shared-memory segment for a shared path. Swept on the next run.
.pgtest*/
