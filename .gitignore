node_modules/
dist/
.env
.env.local
.env.*.local
.DS_Store
*.log
uploads/*
!uploads/.gitkeep

# Throwaway Postgres data directory for npm run test:integration.
# Deleted by the runner, but a killed run leaves it behind.
.pgtest/
